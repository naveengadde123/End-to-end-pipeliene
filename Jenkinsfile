pipeline {
    agent any

    stages {
        stage('Test Jenkins') {
            steps {
                echo 'Jenkins pipeline is running'
                sh 'echo "Hello from Jenkins on EC2"'
            }
        }
    }
}
